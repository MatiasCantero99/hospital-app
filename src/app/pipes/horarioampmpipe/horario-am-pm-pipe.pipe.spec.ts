import { HorarioAmPmPipe } from './horario-am-pm-pipe.pipe';

describe('HorarioAmPmPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HorarioAmPmPipe();
    expect(pipe).toBeTruthy();
  });
});
