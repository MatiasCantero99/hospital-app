import { EstadoturnocolorDirective } from './estadoturnocolor.directive';

describe('EstadoturnocolorDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoturnocolorDirective();
    expect(directive).toBeTruthy();
  });
});
