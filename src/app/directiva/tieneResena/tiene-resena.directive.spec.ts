import { TieneResenaDirective } from './tiene-resena.directive';

describe('TieneResenaDirective', () => {
  it('should create an instance', () => {
    const directive = new TieneResenaDirective();
    expect(directive).toBeTruthy();
  });
});
