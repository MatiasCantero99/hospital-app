import { ResaltarRolDirective } from './resaltar-rol.directive';

describe('ResaltarRolDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarRolDirective();
    expect(directive).toBeTruthy();
  });
});
